name: skill-package

on:
  pull_request:
    paths:
      - "SKILL.md"
      - "references/**"
      - "contracts/**"
      - "tests/**"
      - "detector/**"
      - "dist/**"
      - "README.md"
      - "AGENTS.md"
      - "scripts/**"
      - "package.json"
      - ".github/workflows/skill-package.yml"
  push:
    branches: [main]
    paths:
      - "SKILL.md"
      - "references/**"
      - "contracts/**"
      - "tests/**"
      - "detector/**"
      - "dist/**"
      - "README.md"
      - "AGENTS.md"
      - "scripts/**"
      - "package.json"
      - ".github/workflows/skill-package.yml"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Validate contract JSON
        run: |
          python3 -m json.tool contracts/public-contract.json > /dev/null
          python3 -m json.tool contracts/surface-categories.json > /dev/null

      - name: Check README surface-pattern count
        run: bash scripts/check-pattern-count.sh

      - name: Regenerate Cursor distributions
        run: npm run build

      - name: Run full validation
        run: npm test

      - name: Test deployment and rollback transaction
        run: npm run test:deploy

      - name: Fail if generated distributions drifted
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::Generated Cursor distributions are out of sync."
            echo "Run 'npm run build' and commit the result."
            git status --short
            exit 1
          fi
          echo "generated Cursor distributions are in sync"
